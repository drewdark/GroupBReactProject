import React from 'react';
import PropTypes from 'prop-types';
import './Delete.css';

const Delete = () => (
  <div className="Delete">
    Delete Component
  </div>
);

Delete.propTypes = {};

Delete.defaultProps = {};

export default Delete;
