import React from 'react';
import PropTypes from 'prop-types';
import './Update.css';

const Update = () => (
  <div className="Update">
    Update Component
  </div>
);

Update.propTypes = {};

Update.defaultProps = {};

export default Update;
